# App
NODE_ENV=development
PORT=3000
APP_ORIGIN=http://localhost:3000

# Auth (required)
JWT_SECRET=replace-with-a-long-random-secret

# Database (MongoDB)
DATABASE_URL=mongodb://127.0.0.1:27017/felfelchat

# Rust server
SERVE_FRONTEND=true
NEXT_INTERNAL_PORT=3001

# Uploads
UPLOAD_DIR=./uploads
UPLOAD_MAX_SIZE_MB=20

# Backups
BACKUP_DIR=./backups
BACKUP_SIGNING_KEY=replace-with-a-long-random-signing-key

# Audit logs
AUDIT_LOG_DIR=./logs

# Monitoring (optional)
SENTRY_DSN=
SENTRY_TRACES_SAMPLE_RATE=0.1

# WebRTC TURN (optional but recommended for voice calls)
NEXT_PUBLIC_WEBRTC_TURN_URLS=
NEXT_PUBLIC_WEBRTC_TURN_USERNAME=
NEXT_PUBLIC_WEBRTC_TURN_CREDENTIAL=
